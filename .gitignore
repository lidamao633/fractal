# OS / editors
.DS_Store
.vscode/
.idea/

# Logs
*.log

# Internal notes — not for public repo
plans/
docs/
dist/

# Release/build tooling IS tracked (reproducible releases) — do NOT re-ignore:
#   scripts/release.mjs, packaging/, tests/, hooks/hooks.codex.json

# Note: this plugin has zero runtime dependencies — no node_modules or build artifacts.
# .nav/.cache/ and .nav/*.index.json in user projects are handled by nav init
# writing to each project's own .gitignore.
